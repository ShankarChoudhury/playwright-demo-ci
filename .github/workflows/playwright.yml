name: Playwright Tests

on:
  workflow_dispatch:
    inputs:
      browser:
        description: 'Browser to run the tests'
        required: false
        type: choice
        default: 'chromium'
        options:
          - chromium
          - firefox
          - webkit
          - 'Microsoft Edge'
          - ''
      build_version:
        description: 'Version tag to deploy'
        required: false
        default: 'v1.0.0'
        type: string

      tag:
        description: 'Test tag to run'
        required: false
        default: ''
        type: string  

      retry:
        description: 'Retry for failed tests'
        required: false
        default: 0
        type: number

  push:
    branches:
      - main
      - master

  pull_request:
    branches:
      - main
      - master

jobs:
  TestExecution:
    timeout-minutes: 60
    runs-on: ubuntu-latest
    container:
      image: mcr.microsoft.com/playwright:v1.62.0-noble
      options: --user 1001

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: lts/*

      - name: Install dependencies
        run: npm ci

      - name: Install Playwright Browsers
        run: sudo npx playwright install --with-deps

      - name: Run Playwright tests
        shell: bash
        run: |
          BROWSER="${{ github.event.inputs.browser || 'chromium' }}"
          TAG="${{ github.event.inputs.tag || '' }}"
          RETRY="${{ github.event.inputs.retry || 0 }}"

          echo "Playwright tests started for browser: $BROWSER"

          if [ -n "$TAG" ]; then
            echo "Running tests with tag: $TAG and retries: $RETRY"
            npx playwright test orangespecs --project="$BROWSER" --grep "$TAG" --retries="$RETRY"
          else
            echo "No tag provided. Running all tests with retries: $RETRY"
            npx playwright test orangespecs --project="$BROWSER" --retries="$RETRY"
          fi
      - uses: actions/upload-artifact@v4
        if: ${{ !cancelled() }}
        with:
          name: playwright-report
          path: playwright-report/
          retention-days: 15
